# --- SICHERHEIT (Niemals hochladen!) ---
credentials.json
.env
last_backup.txt
client_secrets.json

# --- DATENBANKEN ---
*.db
# Wir ignorieren nicht alle JSONs, sondern nur die sensiblen
# ebod_atlas_backup.json MUSS hochgeladen werden, damit die App läuft.
# Falls du aber eine JSON mit privaten Notizen hast, füge sie hier einzeln ein.

# --- PYTHON & TOOLS ---
__pycache__/
*.py[cod]
.streamlit/config.toml
# Die secrets.toml ignorieren wir, da wir die Secrets direkt in Streamlit Cloud eingeben
.streamlit/secrets.toml

# --- GEHÄUSE & BETRIEBSSYSTEM ---
.DS_Store
Thumbs.db